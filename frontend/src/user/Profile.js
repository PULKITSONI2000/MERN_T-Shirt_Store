import React from "react";
import moduleName from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile Page">
      <h1>This is Profile page</h1>
    </Base>
  );
};

export default Profile;
